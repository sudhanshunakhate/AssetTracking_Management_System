# Profile 2 — schema caits (structure + admin seed)
# Activate: CAITS_DB=2  or  spring.profiles.active=2  (default)
spring:
  config:
    activate:
      on-profile: "2"
  datasource:
    url: jdbc:postgresql://192.168.0.223:5432/caits?currentSchema=caits
    username: caits
    password: caits123
  jpa:
    properties:
      hibernate:
        default_schema: caits

caits:
  db:
    id: 2
    schema: caits
