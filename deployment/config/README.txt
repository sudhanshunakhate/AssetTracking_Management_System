Placeholders only — never put real passwords here.

- environment.example     — list of CAITS_* / Spring env vars
- setenv.bat.example      — Windows Tomcat bin/setenv.bat template
- setenv.sh.example       — Linux Tomcat bin/setenv.sh template

Copy the appropriate setenv file into $CATALINA_HOME/bin/ and edit values on the server.
